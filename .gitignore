# 依赖
node_modules/

# ---- 本地开发日志 / 环境转储：不上传 ----
# 这些文件含本机绝对路径、本机配置快照与开发过程痕迹，只留在本地。
BUILD.md
dump-config.txt
dump-*.txt
*.dump.txt
*.local.md
*.local.json

# ---- 本地开发 overlay ----
# cordis.yml 里的插件行必然包含**你本机 clone 的绝对路径**，因此不跟踪；
# 可跟踪的模板见 cordis.example.yml（占位路径 + 用法说明）。
cordis.yml

# 日志与临时文件
*.log
*.tmp
*.bak
.tsbuildinfo

# 编辑器 / 系统
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
